# Normalise to LF in the repository.
* text=auto

# These are read by Linux tools and must keep LF on checkout everywhere.
*.sh      text eol=lf
*.conf    text eol=lf
Caddyfile text eol=lf

*.woff2 binary
